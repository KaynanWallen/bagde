# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bagde_db
DB_USER=postgres
DB_PASSWORD=postgres

# Or use connection string (Sequelize will use this if provided)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/bagde_db

# Server Configuration
PORT=3000
NODE_ENV=development
